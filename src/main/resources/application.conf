ktor {
    deployment {
        port = 8068
        port = ${?PORT}
    }
    application {
        modules = [ me.nathanfallet.suitebde.ApplicationKt.module ]
    }
    environment = "localhost"
    environment = ${?KTOR_ENV}
}
database {
    protocol = "mysql"
    host = "localhost"
    name = "suitebde"
    user = "root"
    password = ""
    host = ${?DB_HOST}
    name = ${?DB_NAME}
    user = ${?DB_USER}
    password = ${?DB_PASSWORD}
}
jwt {
    secret = "secret"
    secret = ${?JWT_SECRET}
    issuer = "suitebde"
    audience = "suitebde"
}
